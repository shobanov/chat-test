import { Form } from 'formik';
import styled from 'styled-components';

export const FormContainer = styled(Form)`
  width: 100%;
  display: flex;
  min-height: 69px;
  position: fixed;
  bottom: 0;
`;